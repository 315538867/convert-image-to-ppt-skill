{
  "name": "image-to-ppt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "基于 V2 契约流水线的高还原度可编辑图片转 PowerPoint 引擎",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "npm run build:skill",
    "build:skill": "node scripts/build-skill.mjs",
    "install:skill": "npm run build:skill && node scripts/install-skill.mjs",
    "preflight": "node packages/cli/src/preflight.mjs",
    "validate:contracts": "node packages/core/src/validate-v2-contracts.mjs packages/core/examples/v2/minimal-single-page.json",
    "test:core": "node --test tests/unit/*.test.mjs",
    "test:integration": "node tests/integration/smoke.mjs",
    "test:skill": "node scripts/test-built-skill.mjs",
    "test:golden-corpus": "node scripts/check-golden-corpus.mjs",
    "test:golden-corpus:full": "node scripts/check-golden-corpus.mjs --full",
    "test": "npm run preflight && npm run test:core && npm run test:integration && npm run build:skill && npm run test:skill",
    "audit": "npm run test:golden-corpus && npm audit --audit-level=high"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
